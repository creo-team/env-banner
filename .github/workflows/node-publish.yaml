name: Node Publish
run-name: >-
  Publish [${{ github.event.release.tag_name || github.ref_name }}]

on:
  release:
    types: [published]
  push:
    branches: '*'
  workflow_dispatch:

jobs:
  publish:
    runs-on: creo-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: 'https://registry.npmjs.org'
      - name: Install Dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Publish to npm
        if: github.event_name == 'release'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
      - name: Dry Run (not a release)
        if: github.event_name != 'release'
        run: npm publish --access public --dry-run